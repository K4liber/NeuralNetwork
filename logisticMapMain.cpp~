#include "lib.h"

using namespace std;

int main(int argc,char *argv[])
{
  double epsilon;
  
  if(argv[1])
    epsilon = atof(argv[1]);
  else
    epsilon = 0.01;

  logisticMap(epsilon);

  return( 0 );
  
}


